{
  "name": "pandoc-server",
  "version": "0.0.4",
  "description": "Take binary docx from POST, return Markdown.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adriano Ferrari",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "pdc": "^0.2.3"
  }
}
